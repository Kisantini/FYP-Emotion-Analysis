Trained BERT model files are not uploaded due to GitHub file size limitations.

The model can be regenerated by running:
notebooks/training.ipynb

This follows standard machine learning deployment practice.
